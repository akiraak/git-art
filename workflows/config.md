# 記事設定の収集

## 前提

- リポジトリが `projects/{プロジェクト名}/repo/` にクローン済みであること

## 手順

1. 利用者に「どのような記事にしたいですか？」と自由入力で質問する
   - 例: 「初心者向けにカジュアルな紹介記事を書きたい」「技術的な解説記事にしたい」など
2. 回答を `projects/{プロジェクト名}/config.json` に保存する

## config.json の形式

```json
{
  "description": "利用者が入力した記事の方向性をそのまま記録する"
}
```
